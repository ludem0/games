services:
  app:
    build: ./app
    volumes:
      - ./users.json:/app/users.json
      - ./seasons.json:/app/seasons.json
      - ./minigames.json:/app/minigames.json
      - ./chats.json:/app/chats.json
      - ./letterbox.json:/app/letterbox.json
      - ./doubleteam.json:/app/doubleteam.json
      - ./ultimate.json:/app/ultimate.json
      - ./avatars:/app/public/avatars
    env_file: .env
    networks:
      - internal

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - internal

networks:
  internal:
    driver: bridge
